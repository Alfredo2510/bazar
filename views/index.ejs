<%include _header%>

<%include _slider%>
 <!-- <a href="/registro-p" class="btn btn-primary pull-right">Registrar nuevo celular</a> -->
<br>
<style>
    .padre{
        text-align: center;
    }
    .panel-default{
        width: 300px;
        margin: 6px 19px;
        display: inline-block;
        background:  #f0f8ff00;

    }
    .panel-body {
    text-align: center;
    padding: 0px;
}
.panel-default>.panel-heading{
    background: #f0f8ff00;
    padding: 0px;
}
h4{
    font-size: 13px;
}
</style>
<div class="padre">
    <h1>Productos destacados</h1>
<% pro.forEach((e) => {%>
    <div class="panel panel-default">
        <div class="panel-heading">
            <%if (e.name) {%>   
            <% var source = "/uploads/myImage-" %>
            <% source += e.ID %>
            <% source += ".png" %>    
            <img src='<%= source %>' style="width 120px; height: 171px;">
            <% } else {%>
            <h3>Sin nombre</h3>
            <% } %>
        </div>
        <div class="panel-body">            
            <h3> <%= e.name%></h3>
            <h4>Precio: $<%= e.money %> </h4>
            <h4>Categoria: <%= e.category %></h4>
            <h4> <%= e.description %></h4>

     
        </div>
    </div>

    <% }) %>
</div>
    <%include _footer%>